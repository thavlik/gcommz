package relay

import (
	"context"
	"fmt"

	"github.com/thavlik/gcommz/components/relay/pkg/api"
)

func (s *Server) Kick(ctx context.Context, req api.KickRequest) (*api.KickResponse, error) {
	return nil, fmt.Errorf("unimplemented")
}
