package relay

import (
	"context"
	"fmt"

	"github.com/thavlik/gcommz/components/relay/pkg/api"
)

func (s *Server) CreateChannel(ctx context.Context, req api.CreateChannelRequest) (*api.CreateChannelResponse, error) {
	return nil, fmt.Errorf("unimplemented")
}
